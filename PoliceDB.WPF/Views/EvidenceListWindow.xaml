<Window
    x:Class="PoliceDB.WPF.Views.EvidenceListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Просмотр улик"
    Width="800"
    Height="600"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <Window.Resources>
        <!--  Стиль для кнопки улики  -->
        <Style x:Key="EvidenceButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#FF0078D7" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,5,0,5" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F7FF" />
                                <Setter Property="BorderBrush" Value="#005A9E" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E1EFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Шапка  -->
        <Border
            Grid.Row="0"
            Padding="10"
            Background="#FF0078D7"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  Кнопка назад  -->
                <Button
                    Grid.Column="0"
                    Width="80"
                    Height="30"
                    Background="White"
                    Command="{Binding BackCommand}"
                    Content="← Назад"
                    Cursor="Hand"
                    FontWeight="Bold"
                    Foreground="#FF0078D7" />

                <!--  Заголовок  -->
                <TextBlock
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="Список улик" />

                <!--  Кнопка обновления  -->
                <Button
                    Grid.Column="2"
                    Width="100"
                    Height="30"
                    Background="White"
                    Command="{Binding RefreshCommand}"
                    Content="Обновить"
                    Cursor="Hand"
                    FontWeight="Bold"
                    Foreground="#FF0078D7" />
            </Grid>
        </Border>

        <!--  Основной контент  -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <!--  Список улик (если есть)  -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasEvidences}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>

                <StackPanel>
                    <ItemsControl ItemsSource="{Binding Evidences}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    Command="{Binding ViewDetailsCommand}"
                                    CommandParameter="{Binding Id}"
                                    Style="{StaticResource EvidenceButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                                        <TextBlock
                                            Margin="10,0,0,0"
                                            Foreground="#666666"
                                            Text="{Binding Code, StringFormat=' ({0})'}" />
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            Foreground="#666666"
                                            Text="{Binding Type}" />
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            Foreground="#666666"
                                            Text="{Binding DiscoveryDate, StringFormat='Обнаружена: {0:dd.MM.yyyy в HH:mm}'}" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!--  Сообщение об отсутствии улик  -->
            <Border
                Padding="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#F9F9F9"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="5">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding NoEvidences}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="#666666"
                        Text="Нет улик"
                        TextAlignment="Center" />
                    <TextBlock
                        Margin="0,10,0,0"
                        Foreground="#666666"
                        Text="По данному делу ещё не добавлены улики"
                        TextAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!--  Статистика  -->
        <Border
            Grid.Row="2"
            Margin="0,10,0,0"
            Padding="10"
            Background="#F5F5F5"
            CornerRadius="5">
            <TextBlock
                HorizontalAlignment="Center"
                FontSize="12"
                Foreground="#666666"
                Text="{Binding Evidences.Count, StringFormat='Всего улик: {0}'}" />
        </Border>
    </Grid>
</Window>