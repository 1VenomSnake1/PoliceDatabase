<Window
    x:Class="PoliceDB.WPF.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Полицейская база данных - Главное меню"
    Width="900"
    Height="700"
    ResizeMode="CanMinimize"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  Здесь можно добавить ссылку на файл стилей  -->
                <!--<ResourceDictionary Source="/PoliceDB.WPF;component/Styles/MainWindowStyles.xaml" />-->
            </ResourceDictionary.MergedDictionaries>

            <!--  Встроенные стили для простоты  -->
            <Style x:Key="MenuButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#FF0078D7" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="#333333" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                                <ContentPresenter
                                    Margin="10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F0F7FF" />
                                    <Setter Property="BorderBrush" Value="#005A9E" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#E1EFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="LogoutButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#FF6B6B" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter
                                    Margin="10,5"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF5252" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF3D3D" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Шапка  -->
        <Border
            Grid.Row="0"
            Padding="15"
            Background="#FF0078D7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="White"
                        Text="Полицейская база данных" />
                    <TextBlock
                        Margin="0,5,0,0"
                        FontSize="14"
                        Foreground="White"
                        Text="{Binding CaseInfo}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="#E6F7FF"
                        Text="{Binding UserInfo}" />
                </StackPanel>

                <!--  Кнопка назад (скрыта на главном экране)  -->
                <Button
                    x:Name="BackButton"
                    Grid.Column="1"
                    Width="100"
                    Height="35"
                    Content="Назад"
                    Visibility="Collapsed" />
            </Grid>
        </Border>

        <!--  Основное содержимое  -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="30">
                <ItemsControl ItemsSource="{Binding MenuItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button
                                Width="160"
                                Height="160"
                                Margin="15"
                                Command="{Binding Command}"
                                Style="{StaticResource MenuButtonStyle}">
                                <StackPanel>
                                    <!--  Иконка  -->
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        FontSize="48"
                                        Text="{Binding Icon}" />
                                    <!--  Заголовок  -->
                                    <TextBlock
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Center"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Text="{Binding Title}"
                                        TextWrapping="Wrap" />
                                    <!--  Описание  -->
                                    <TextBlock
                                        Margin="0,5,0,0"
                                        HorizontalAlignment="Center"
                                        FontSize="12"
                                        Foreground="Gray"
                                        Text="{Binding Description}"
                                        TextAlignment="Center"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>

        <!--  Нижняя панель  -->
        <Border
            Grid.Row="2"
            Padding="10"
            Background="#F0F0F0">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    x:Name="LogoutButton"
                    Width="100"
                    Height="35"
                    Click="LogoutButton_Click"
                    Content="Выйти"
                    Style="{StaticResource LogoutButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>